fastapi==0.115.6
uvicorn[standard]==0.34.0
pydantic==2.10.4
httpx==0.28.1
pytest==8.3.4
hypothesis==6.122.3

# Document upload (PDF/PPTX text extraction) + persistence.
python-multipart==0.0.20
pypdf==5.1.0
python-pptx==1.0.2
SQLAlchemy==2.0.36
# Postgres driver for production DATABASE_URL (e.g. Render/DigitalOcean
# managed Postgres). Not needed for the local SQLite default.
psycopg2-binary==2.9.10
