"""Deal-Sourcing & CRM Enrichment Agent (TransformBiz).

A deterministic, batch-oriented processing engine that transforms pre-fetched
source items into a strict JSON output contract. The package is intentionally
non-scraping: it performs no outbound network requests during processing.
"""

__all__ = ["__version__"]

__version__ = "1.0.0"
